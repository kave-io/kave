# Developer compose only. Generate two independent values; never reuse these
# keys across environments and never commit the populated .env file.
KAVE_RUNTIME_MASTER_KEY=
KAVE_RUNTIME_SECRET_IDEMPOTENCY_KEY=

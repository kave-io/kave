// Package audit defines the immutable audit log model.
// Audit is cross-cutting between control and runtime planes — it records mutations
// to policies, credentials, agents, and runs.
package audit
